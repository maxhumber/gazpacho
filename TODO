fix soup dir
post request (dont add to soup get)

soup tests:
  fix all old remove_tags references
  deprecation for remove_tags
  deprecation for strict
  new .strip()
  new random return type
  new last return type
  test value error return
  void tag images (original problem)
  get init cls method

utils tests:
  sanitize
  HTTPError


----

make sure types are working with mypy

mypy auto checking
black auto
monkeytype auto apply


conda releases

auto reverse matching?
string matching on attrs

---

monkeytype run `which pytest`
monkeytype apply gazpacho
